// the OTA tool for pcduino remote server 
// in some cases the user want to get the  
// fresh new feature without 'restart' their
// machine, this works to make this 


var actionMap = new Map
{
	// temporarily 4 kinds of actions
	Add : 1;
	Delete: 2;
	Modify: 3;
	Move: 4;

}

var actionRes = new Res
{
	// the files array
	files[] = new files{};
	
	
}

// the actionRes is the resources the action want to 
// have effect on, like one file
function update(action,actionRes,){



}