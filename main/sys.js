// this is temporarily added here , will 
// move to src dir later when ready 

function init(app){

	// here this is just a context for app to execute 
	app.apply(this);

}






module.exports = {
	init : init 
}


