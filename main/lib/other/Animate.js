
vra global = {

	var ctx; // the context to paint
	
}

function prepare(a concrete function called A, the time interval){
	
	setInterval(function A, the intervaltime);
	
}

function init(document){
	// param: 
	/*	
		document: to get the painting context	
	*/
	// the animate element must exist
	var ctx = document.getElementById("animate").getContext("2d");
	
	
}

